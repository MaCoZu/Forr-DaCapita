---
interface Props {
	date: Date;
}

const { date } = Astro.props;
console.log(date);
---

<time datetime={date.toISOString()}>
	{
		date.toLocaleDateString("en-us", {
			weekday: "short",
			year: "numeric",
			month: "short",
			day: "numeric",
		})
	}
</time>
